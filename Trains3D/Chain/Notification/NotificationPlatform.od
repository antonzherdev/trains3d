package core.chain

stub class NotificationCenter {
    static val instance : NotificationCenter = NotificationCenter()

    def addObserver<D>(name : string, block : D -> void) : NotificationObserver

    def post<D>(name : string, data : D)
}

stub class NotificationObserver {
    def detach
}

