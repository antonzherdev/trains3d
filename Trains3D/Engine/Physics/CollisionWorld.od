package com.antonzherdev.eg

stub class CollisionWorld<T> extends PhysicsWorld<T> {
    def add(body : CollisionBody<T>)
    def remove(body : CollisionBody<T>)
    def remove(item : T)
    def bodyFor(item : T) : CollisionBody<T>?
    def clear

    def detect : Iterable<Collision<T>>
    def closestCrossPointWith(segment : Line3) : CrossPoint<T>?
    def crossPointsWith(segment : Line3) : [CrossPoint<T>]
}

