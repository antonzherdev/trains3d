import EGTypes

stub class EGMapSsoTileIndex<T>(mapSize : EGSizeI) {
    def lookupFor(tile : EGPointI) : T?
    def lookupWith(def : () -> T, forTile : EGPointI) : T
    def set(object : T, forTile : EGPointI) : T
    def values : [T]
    def clear
}