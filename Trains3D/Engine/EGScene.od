import EGCamera
import EGTypes

stub class EGScene {
    var camera : EGCamera |
        set = {
            self.camera = camera
            camera.reshapeWith(lastSize)
        }
    private var lastSize : CGSize

    def reshapeWith(size : CGSize) {
        lastSize = size
        camera.reshapeWith(size)
    }

    def draw {
        camera.startDraw
    }
}