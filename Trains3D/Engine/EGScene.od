import "EGCamera.h"

stub struct CGSize(width : float, height : float)

stub EGCamera {
    def startDraw

    def reshapeWith(size : CGSize)
}

class EGScene {
    var camera : EGCamera

    def reshapeWith(size : CGSize) {
        camera.reshapeWith(size)
    }

    def draw {
        camera.startDraw
    }
}