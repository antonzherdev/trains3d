package com.antonzherdev.eg


stub class TTFFont(name : string, override size : uint) extends Font {
    private var symbols = MHashMap<char, FontSymbolDesc>()

    override def symbolOpt(smb : char) : FontSymbolDesc? = symbols.objectFor(smb, orUpdateWith = symbol(smb))

    def symbol(smb : char) : FontSymbolDesc

    override def texture : React<Texture>

    private def generateTexture : Texture
}