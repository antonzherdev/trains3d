package com.antonzherdev.eg

enum Platform(shadows : bool, touch : bool) {
    MacOS(true, false)
    iOS(true, true)
}

enum InterfaceIdiom(isPhone : bool, isPad : bool, isComputer : bool) {
    phone(true, false, false)
    pad(false, true, false)
    computer(false, false, true)
}