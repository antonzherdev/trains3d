import CNCache

import EGTexture

class EGContext {
    private val textureCache = CNCache.new<string, EGTexture>

    def textureFor(file : string) : EGTexture = textureCache.lookupWith(init = EGTexture(file), forKey = file)
}