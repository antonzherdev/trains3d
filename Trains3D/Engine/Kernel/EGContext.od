import EGTexture
import CNCache

class EGContext {
    private val textureCache = CNCache<string, EGTexture>

    def textureFor(file : string) : EGTexture = textureCache.lookupWith(def = EGTexture(file), forKey = file)
}