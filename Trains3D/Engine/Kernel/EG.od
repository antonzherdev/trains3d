import EGDirector
import EGContext
import EGTexture

class EG {
    static def director : EGDirector = EGDirector.current
    static def context : EGContext = EGDirector.current.context
    static def textureFor(file : string) : EGTexture = context.textureFor(file)
}
