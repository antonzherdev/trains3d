import TRColor
import TRTypes
import EGTypes
import EGMap

/*
 * angle - поворот по часовой стрелке. При 0 город направлен по оси x в положительном направлении
 */
class TRCity(color : TRColor, tile : EGMapPoint, angle : int)