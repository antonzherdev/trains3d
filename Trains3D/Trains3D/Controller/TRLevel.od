import EGMap
import TRCity
import TRTypes

class TRLevel(mapSize : EGMapSize) {
    var cities : [TRCity] | private set

    def addNextCity {
        cities += TRCity(TRColor[cities.count], egMapSsoPartialTiles(mapSize).exclude(cities.map(a : TRCity -> a.tile)).randomItem)
    }
}