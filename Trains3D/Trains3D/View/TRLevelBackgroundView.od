import EG
import EGTexture
import TRLevel
import EGMapIso
import EGMaterial

class TRLevelBackgroundView {
    def draw(level : TRLevel) {
        EGMaterial.grass.set
        EG.textureFor("Grass.png").draw(level.map.drawPlane)
    }
}