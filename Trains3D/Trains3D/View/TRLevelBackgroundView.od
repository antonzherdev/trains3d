import EG
import EGTexture
import TRLevel
import EGMapIso
import EGMaterial

class TRLevelBackgroundView(map : EGMapSso) {
    val mapView = EGMapSsoView(map)
    val material = EGStandardMaterial(EG.textureFor("Grass.png"))

    def draw {
        mapView.drawPlaneWith(material)
    }
}