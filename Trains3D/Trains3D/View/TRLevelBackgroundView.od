import EG
import EGTexture
import TRLevel
import EGMapIso
import EGMaterial

class TRLevelBackgroundView(map : EGMapSso) {
    val mapView = EGMapSsoView(map)

    def draw {
        EGMaterial.grass.set
        EG.textureFor("Grass.png").draw(mapView.drawPlane)
    }
}