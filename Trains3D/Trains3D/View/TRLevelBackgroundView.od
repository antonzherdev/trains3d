import EGTexture
import TRLevel
import EGMap

class TRLevelBackgroundView {
    private val texture = EGTexture("Grass.png")

    def draw(level : TRLevel) {
        texture.with(egMapSsoDrawPlane(level.mapSize))
    }
}