import EG
import EGTexture
import TRLevel
import EGMapIso
import EGMaterial

class TRLevelBackgroundView(map : EGMapSso) {
    val mapView = EGMapSsoView(map)

    def draw {
        mapView.drawPlaneWith(material = EGTexture("Grass.png"))
    }
}